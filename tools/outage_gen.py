import os
from pathlib import Path
import pandas as pd
import geopandas as gpd

_YEARS = []

# can't actually do this until I have a connectivity model...
# we need to have downstream customer counts at the device...

def generate_outages(path_to_devices:str):


  return
